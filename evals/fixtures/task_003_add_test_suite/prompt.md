This CLI has no tests. Add a pytest suite that covers the parse and format
subcommands for both the happy path and error cases (missing file, invalid
JSON). Use only stdlib and pytest — no additional plugins.
