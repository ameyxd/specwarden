# json-cli

A small command-line tool with two subcommands:

- `parse <file>` — prints each top-level key and its Python type.
- `format <file>` — pretty-prints the JSON with 2-space indentation.

## Usage

```
python cli.py parse data.json
python cli.py format data.json
```

## Tests

There are no tests yet.
